{
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "lint": "oxlint ."
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@rspack/core": "^1.2.0",
    "@rspack/cli": "^1.2.0",
    "oxlint": "^1.0.0"
  }
}
