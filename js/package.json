{
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "lint": "oxlint ."
  },
  "devDependencies": {
    "@microsoft/api-extractor": "^7.58.2",
    "@rslib/core": "^0.21.0",
    "@rspack/cli": "^1.2.0",
    "@rspack/core": "^1.2.0",
    "oxlint": "^1.0.0",
    "typescript": "^5.7.0"
  }
}
