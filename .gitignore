# Rust
/target/
**/target/
# Cargo.lock is committed for reproducible CI
*.rs.bk

# wasm-pack (website wasm cdylib — built by the dev server's WasmBuildPlugin)
demo/website/native/pkg/
demo/website/native/target/

# Node
node_modules/
dist/
*.tsbuildinfo

# Generated TypeScript
demo/playground-view/src/cases/generated.ts

# Build output
js/packages/tur-rspack-plugin/dist/

# Test fixtures
scripts/tests/fixtures/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Env
.env
.env.local

# Local CI
.actrc
logs/

# Wrangler
.wrangler/

# Playwright test results
test-results/
.playwright-mcp/

# Android (Compose integration + demo app)
#
# Gradle/AGP build outputs and per-machine SDK config. These live under the
# standalone gradle roots demo/compose and integrations/compose (each has its
# own settings.gradle.kts). `**/build/` and `**/.gradle/` have no tracked
# collisions in this repo, so the broad globs are safe.
**/build/
**/.gradle/
**/.kotlin/
**/local.properties
**/captures/

# Generated native libraries — produced by the `buildDemoNative`/`copyDemoNative`
# gradle tasks (cargo ndk → src/main/jniLibs/<abi>/libtur_demo.so). Do NOT
# commit; rebuilt on every assemble.
demo/compose/src/main/jniLibs/

# Generated JS asset — produced by the `buildPlaygroundJs`/`copyPlaygroundJs`
# gradle tasks (pnpm --filter @tur/playground-view build → assets/playground.js).
# Scoped: only the demo app's asset, not any other file named playground.js.
demo/compose/src/main/assets/playground.js
.zcode/
